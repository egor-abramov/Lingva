input:
    lang: Python
    code: |
        import urllib.request

        try:
            response = urllib.request.urlopen('http://httpbin.org/get', timeout=2)
            print("SUCCESS: " + str(response.status))
        except Exception as e:
            print("FAILED: " + str(e))
    stdin: []
output:
    - data: |
        FAILED: <urlopen error [Errno -3] Try again>
