input:
    lang: Go
    code: |
        package main

        import "fmt"

        func divide(a, b int) int {
            return a / b
        }

        func main() {
            fmt.Println("Starting calculation...")
            a, b := 10, 0
            result := divide(a, b)
            fmt.Println("This line will never be reached.", result)
        }
    stdin: []
output:
    stdout: |
        Starting calculation...
    stderr: |
        panic: runtime error: integer divide by zero

        goroutine 1 [running]:
        main.divide(...)
        	/workspace/main.go:6
        main.main()
        	/workspace/main.go:12 +0x4c
        exit status 2
