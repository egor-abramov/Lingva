input:
    lang: Go
    code: |
        package main

        import (
            "fmt"
            "os"
        )

        func main() {
            filePath := "/tmp/hacked.txt"
            err := os.WriteFile(filePath, []byte("You have been compromised!"), 0644)
            if err != nil {
                fmt.Printf("OS Error: %v\n", err)
                return
            }
            fmt.Println("File written successfully.")
        }
    stdin: []
output:
    stdout: |
        OS Error: open /tmp/hacked.txt: read-only file system
    stderr: ""
